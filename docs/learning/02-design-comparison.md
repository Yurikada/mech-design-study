# 講義2：制約を満たす設計案を比較する

[一問一答と解説](#一問一答と解説)

M1では、複数の設計案を同じ条件で計算し、選んだ理由を説明する。最初の題材は、加熱される片持ち支持板の軽量化。個別技術として寸法依存・感度・支配制約を、統合技術として比較条件・評価結果・計算の来歴を学ぶ。

[講義1](01-lecture.md) · [ロードマップ](../roadmap.md) · [統合の構造](../architecture.md)

AI支援で作成した学習用資料。物性・要求は架空の教材条件であり、実機の安全性を保証しない。学習者個人の解答・会話履歴は含めない。

## 1. 目的・制約・設計変数を分ける

![長さL、幅b、厚みtと、完全固定の根元・先端横荷重Fの関係](assets/cantilever.svg)

| 分類 | 今回の設定 | 判断での役割 |
|---|---|---|
| 目的 | 質量を小さくする | 制約を満たす候補どうしを比較する |
| 機能上の制約 | 根元から100 mmの位置で先端の荷重を支持する。この直線梁ではL = 100 mm | 軽くても所定の位置に届かなければ候補に残せない |
| 寸法の制約 | 幅b ≤ 45 mm、厚みt ≤ 4 mm | 設置空間に収める |
| 性能の制約 | 応力 ≤ 30 MPa、たわみ ≤ 0.100 mm、一次固有振動数 ≥ 200 Hz、先端温度 ≤ 313.15 K | それぞれ独立に合否を判定する |
| 設計変数 | 幅b、厚みt | 今回変更してよい寸法。長さ変更案も比較表に載せ、機能制約で判定する |
| 固定する条件 | 材料、静的先端力1 N、発熱量率1 W、根元温度298.15 K、モデルの境界条件と判定閾値 | 寸法を変えた効果を比較できるようにする |

長さ・幅・厚みの要求はこの講義で追加した学習用条件。単一ケースCLIはこれらを判定しない。M1の比較コマンドが共通要求として追加判定する。製造公差はまだ扱わず、長さは要求値との厳密な一致を上下限の2判定で確認する。

質量には今回は上限を設けず、最小化する目的として使う。「質量30 g以下」という予算を追加する場合は、質量を制約としても判定する。目的と制約に同じ物理量を使ってよい。

```mermaid
flowchart TD
  R[共通の要求・材料・荷重・モデル] -->|全案へ適用| C[各案の寸法を入力]
  C -->|機能・寸法と解析結果を確認| J{今回の制約をすべて満たすか}
  J -->|違反あり| N[理由と数値を残して候補から外す]
  J -->|満たす| P[候補どうしの質量を比較する]
  P -->|最も軽い候補を特定| S[今回の範囲で選んだ理由を説明する]
  U[疲労など9分野は未評価] -->|未評価を明記| S
```

制約を先に確認することで、例えば「たわみ違反を軽さで埋め合わせる」という判定を防げる。異なる単位のmarginをそのまま足して総合点にしてはいけない。

## 2. 同じモデルで4案を計算する

基準は `cases/heated_cantilever.toml`。各案は寸法だけを変更する。計算モデルは `analytical_baseline_v1`、先端質量なし・小変形の一様梁、根元温度固定・側面断熱の定常熱伝導である。

| 案 | 変更 | L [mm] | b [mm] | t [mm] | 質量 [g] |
|---|---|---:|---:|---:|---:|
| A | 基準 | 100 | 30 | 3 | 24.30 |
| B | 薄くする | 100 | 30 | 2 | 16.20 |
| C | 幅を広げる | 100 | 45 | 3 | 36.45 |
| D | 短く薄くする | 70 | 30 | 2 | 11.34 |

| 案 | 応力 [MPa] | たわみ [mm] | 一次固有振動数 [Hz] | 先端温度 [K] |
|---|---:|---:|---:|---:|
| A | 2.222 | 0.07055 | 246.757 | 304.803 |
| B | 5.000 | 0.23810 | 164.504 | 308.130 |
| C | 1.481 | 0.04703 | 246.757 | 302.586 |
| D | 3.500 | 0.08167 | 335.723 | 305.136 |

表示値は丸めている。合否判定には丸める前の値を使う。これらの値は既存のPythonモデルで再計算した。Dでは荷重・加熱の作用位置も先端とともに70 mmへ移るため、100 mm位置での支持・加熱の計算結果とは見なせない。

### 最初の確認

1. 今回の制約をすべて満たす候補はどれか。外す案には理由を付ける。
2. 残った候補のうち、軽量化を目的としてどれを選ぶか。
3. 選んだ案を「全分野で合格」「連続的な寸法範囲で最適」と呼べるか。

まず自分で判断し、物理モデルの合否と機能・寸法の合否を分けて説明する。

## 3. 寸法依存から支配制約を考える

材料・荷重・熱入力を一定とした、現在のモデルの比例関係は次のとおり。

| 指標 | 長さLへの依存 | 幅bへの依存 | 厚みtへの依存 |
|---|---|---|---|
| 質量 | L | b | t |
| 曲げ応力 | L | 1/b | 1/t² |
| たわみ | L³ | 1/b | 1/t³ |
| 一次固有振動数 | 1/L² | 依存なし | t |
| 温度上昇 | L | 1/b | 1/t |

例えば幅を広げると、たわみは減るが、この梁モデルの固有振動数は変わらない。長さを短くすれば多くの数値が改善するが、機能を満たす寸法かどうかは別に確認する。

「支配制約」は、ある設計変更を進めたときに、その変更を制限する条件として考える。異なる単位のmarginの数値の小ささだけでは決められない。

次の演習ではL = 100 mm、b = 30 mmを固定し、tを連続的に小さくする。各制約から厚みの下限を求め、最初に境界へ達する制約を説明する。ここでは実際に選べる板厚・公差・疲労などは未評価である。

## 4. 比較機能に保存する情報

| 情報 | 必要な理由 |
|---|---|
| 共通の要求とその版 | 案ごとに合格基準を緩めた結果を混ぜないため |
| 案の識別子・寸法・材料・荷重・熱条件 | 何を変え、何を固定したか確認するため |
| モデル版・境界条件・入力スナップショット | 同じ数値を再現し、適用範囲を判断するため |
| 指標値・単位・閾値・margin・状態・理由 | 合否と未評価を追跡するため |
| 今回の制約を満たす候補か、製品全体の評価状態 | 限定した比較ができることと、製品全体の評価完了を区別するため |

単一ケースの評価器を繰り返し呼び、結果を共通の要求と照合する比較層を設計する。材料や荷重も変える試験を行うなら、その差を明示し、寸法だけの効果とは解釈しない。入力不備・計算エラー・未評価は値0や合格へ変換しない。

## 5. 表を再現する

### 比較コマンドを実行する

リポジトリ直下のPowerShellで実行する。

```powershell
.\run.cmd compare cases\heated_cantilever_comparison.toml
.\run.cmd compare cases\heated_cantilever_comparison.toml --output outputs\comparison.json
.\run.cmd compare --replay outputs\comparison.json --json
```

比較入力の `base_case` は共通の材料・荷重・熱条件・性能閾値、`requirements` は追加の機能・寸法要求、`designs` は各案の寸法を指定する。`requirements_version` は要求の版、各案の `id` は一意な英数字・ハイフン・アンダースコアの識別子。必要なら `requirements.max_mass_kg` を追加して質量上限も判定できる。

| 表示・保存するもの | 意味 |
|---|---|
| candidate = eligible / excluded / error | 今回の制約を満たす候補／違反案／計算不能 |
| physical_overall | 既存の12分野評価の状態。機能・寸法要求は別レコードで保存する |
| FAILの数値・閾値・margin | 候補から外した理由。Dのlength_minなど |
| Not evaluated | 未評価の分野名。数値0や合格に置き換えない |
| Eligible mass order | 今回の制約を満たす候補の質量順 |
| Lightest within study | 最軽量候補のID。同じ計算質量の同率候補はすべて残す |

試行案のいずれかが計算不能なら他案は計算を続けるが、最軽量候補の結論は出さない。入力スキーマが不正なら比較全体を開始しない。長さがモデル適用範囲を外れる場合と、解析できても機能要求に違反する場合は状態を分ける。

比較の終了コードは0 = 候補あり、1 = エラー、2 = 候補なし。`--require-complete` を付けると、候補がある場合でも未評価が残れば3。B・Dが除外されても、A・Cが候補なら通常の終了コードは0になる。

### 条件差と再現性を確かめる

1. 比較TOMLのコピーで、Bの表に `tip_force_n = 0.5` を追加する。案ごとの共通条件上書きが、項目名付きで拒否されることを確認する。
2. 荷重を変えた別の実験をしたい場合は、別の基準ケースと比較TOMLを用意する。同一荷重内で形状の効果を比較する。
3. `--output` で保存したJSONの `input`、`provenance`、各案の評価を確認する。入力ファイル名だけでなく計算時の値が保存される。
4. `--replay` で保存入力から再計算する。元のTOMLは不要。保存済みの結果値を新しい計算結果として使い回さない。

出力ファイルは新規作成のみで、再保存するときは別名にする。コードのSHA-256が保存時と異なる場合は再現計算を拒否する。元のGitリビジョンとPython環境を使って再現する。SHA-256は入力やコードの取り違えを検出するもので、実機モデルの妥当性を証明するものではない。

### 物理量だけをPythonから確認する

セットアップ済みのリポジトリ直下で、以下をPythonとして実行する。A〜Dの入力は基準ケースから生成し、比較表の物理量を表示する。機能・寸法制約の自動判定や案の自動選択はこの例には含めていない。

```python
from dataclasses import replace
from pathlib import Path

from mech_design.case import load_case
from mech_design.evaluation import evaluate

base = load_case(Path("cases/heated_cantilever.toml"))
designs = [
    ("A", 0.100, 0.030, 0.003),
    ("B", 0.100, 0.030, 0.002),
    ("C", 0.100, 0.045, 0.003),
    ("D", 0.070, 0.030, 0.002),
]
print("id,mass_g,stress_MPa,deflection_mm,frequency_Hz,temperature_K,overall")
for name, length, width, thickness in designs:
    beam = replace(
        base.beam, length_m=length, width_m=width, thickness_m=thickness
    )
    result = evaluate(replace(base, name=name, beam=beam))
    values = {item["metric"]: item["value"] for item in result["evaluations"]}
    print(
        f"{name},{result['mass_kg'] * 1000:.2f},"
        f"{values['root_stress'] / 1e6:.3f},"
        f"{values['tip_deflection'] * 1000:.5f},"
        f"{values['first_frequency']:.3f},"
        f"{values['tip_temperature']:.3f},{result['overall_status']}"
    )
```

基準ケース・式・出力構造を変更した場合はこの例を再実行し、表と前提を更新する。元の力学・熱の式と出典は[講義1](01-lecture.md)に記載している。

## M1の完了条件

- 目的、制約、設計変数、固定条件を区別して比較案を説明できる。
- 長さ・幅・厚みを変えた比較を再現し、機能制約も含めて選択理由を示せる。
- 厚みを下げるときの支配制約を、式と計算で説明できる。
- 比較機能で条件の差・結果の来歴・未評価を保持し、その動作を確認できる。

この資料を作成したことや4案を計算したことだけで、M1全体の学習・実装完了とはしない。

<!-- BEGIN GENERATED QA -->
## 一問一答と解説

各問でいったん考え、解答例と解説を開いて確認する。対話で扱った論点を汎用の教材として再構成したもので、個人の回答・成績・実行記録ではない。解答例を読んだことと、自分で説明・実行できたことは別に確認する。

![計算結果と制約判定を分ける評価フロー](assets/evaluation-flow.svg)

### L2-Q01

**問い：** 教材のA・B・C・D案から、制約を満たす最軽量案を選ぶとどうなるか。

<details>
<summary>解答例と解説</summary>

**解答例：** 候補はAとCで、質量の小さいAを選ぶ。

**解説：** AはL=100、b=30、t=3 mm、Bはt=2 mm、Cはb=45 mm（他はAと同じ）、DはL=70、b=30、t=2 mm。Dは必要長さ100 mmを満たさず、Bは周波数だけでなくたわみも違反する。Aの24.30 gはCの36.45 gより軽い。これは共通の教材条件内での選択で、9分野の未評価は残る。

</details>

### L2-Q02

**問い：** A案を薄くして軽量化するとき、たわみと周波数のどちらが先に限界になるか。

<details>
<summary>解答例と解説</summary>

**解答例：** たわみ制約。

**解説：** 他の条件を固定すると、たわみからの厚み下限は約2.6706 mm、周波数からは約2.4315 mm。両方を満たすには下限の大きい方を採る。公称3 mmから削ると先にたわみ制約に達する。これらは現行の付加質量なし・完全固定モデルの結果。

> t_min = max(t_min,δ, t_min,f₁) ≈ 2.6706 mm

</details>

### L2-Q03

**問い：** A案・1 NとC案・0.5 Nを比較して、形状の改善効果を判断できるか。

<details>
<summary>解答例と解説</summary>

**解答例：** その2結果だけでは、形状と荷重の影響が混在する。

**解説：** A×1 N、A×0.5 N、C×1 N、C×0.5 Nの2×2比較を用意する。同じ荷重でA/Cを比較し、同じ形状で荷重差を比較する。共通条件を固定した対の比較によって、1回1変更の考え方を実現する。

</details>

### L2-Q04

**問い：** 比較結果を再現するため、数値結果以外に何を保存するか。

<details>
<summary>解答例と解説</summary>

**解答例：** 計算条件・境界条件・モデル設定と、それらの版を保存する。

**解説：** 寸法、物性、荷重、熱条件、単位、制約、案ID、要求版、ソルバー設定、コードと入力の識別情報を含める。FEMではメッシュ座標・接続・要素種類・次数・積分法・拘束・質量方式も必要。

</details>

### L2-Q05

**問い：** 比較TOMLの個別案へtip_force_nを追加するとunsupported fieldsになるのはなぜか。

<details>
<summary>解答例と解説</summary>

**解答例：** 個別案では寸法だけを変更し、荷重などはbase_caseで共有する仕様だから。

**解説：** 混在した条件を黙って比較しないための入力拒否である。荷重の効果も調べるなら、共通荷重1 Nの比較と0.5 Nの比較を別に用意する。失敗した入力を合格結果として扱わない。

</details>

### L2-Q06

**問い：** 保存JSONの再生は、保存された結果を表示するだけで再現確認になるか。

<details>
<summary>解答例と解説</summary>

**解答例：** ならない。保存入力から再計算して照合する。

**解説：** 現行の--replayはコード版と入力ダイジェストを照合し、元TOMLなしで再計算する。FEM版はNumPy/SciPy版も照合する。版が違う場合は理由を示して拒否する。実行成功と、入力条件が実物に適切かは別の確認。

</details>

### L2-Q07

**問い：** 候補あり・終了コード0なら、未評価の分野も含めて採用決定できるか。

<details>
<summary>解答例と解説</summary>

**解答例：** できない。eligibleは今回の比較条件内の候補という意味。

**解説：** 候補A/Cでもphysical_overallはincomplete。比較の通常終了コード0は計算正常・候補ありを表す。--require-completeでは未評価が残れば3となる。条件違反の案、計算不能の案、未評価を区別して記録する。

</details>
<!-- END GENERATED QA -->
